workflows:
  ios-simulator-browserstack:
    name: Moodle App - Build + BrowserStack
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      xcode: latest
      cocoapods: default
      vars:
        BROWSERSTACK_USERNAME: "paulmwaniki_cvubBk"
        BROWSERSTACK_ACCESS_TOKEN: "GnDb7oSPmJx1RNDJNy2x"
    cache:
      cache_paths:
        - $HOME/.npm
        - node_modules
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
    scripts:
      - name: Check Node version
        script: |
          node -v
          npm -v

      - name: Bypass engine check
        script: |
          # Replace engine-strict=true with false in existing .npmrc
          sed -i '' 's/engine-strict=true/engine-strict=false/g' .npmrc
          # Also set it via npm config globally just in case
          npm config set engine-strict false
          echo "=== .npmrc contents ==="
          cat .npmrc

      - name: Install dependencies
        script: |
          npm ci

      - name: Build Moodle web app
        script: |
          npx gulp
          npm run build:prod

      - name: Capacitor sync iOS
        script: |
          npx cap sync ios

      - name: Build for simulator (no signing)
        script: |
          xcodebuild \
            -workspace ios/App/App.xcworkspace \
            -scheme App \
            -sdk iphonesimulator \
            -configuration Debug \
            -derivedDataPath ios/output \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Upload .app to BrowserStack App Live
        script: |
          APP_PATH=$(find ios/output/Build/Products/Debug-iphonesimulator -name "*.app" | head -1)
          echo "Uploading: $APP_PATH"
          curl -u "$BROWSERSTACK_USERNAME:$BROWSERSTACK_ACCESS_TOKEN" \
            -X POST "https://api-cloud.browserstack.com/app-live/upload" \
            -F "file=@$APP_PATH"

    artifacts:
      - ios/output/Build/Products/Debug-iphonesimulator/*.app
      - /tmp/xcodebuild_logs/*.log
